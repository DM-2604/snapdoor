// Queue name constants — extracted to avoid circular imports between
// queue.module.ts (which imports processors) and processors (which need the queue name).
export const MONTHLY_BILLING_QUEUE = 'monthly-billing';
